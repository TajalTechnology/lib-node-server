import { NodeServer, FrameWorks, IRouter } from '../index';

describe('Validate User role', () => {
    // const nodeServer = new NodeServer(FrameWorks.EXPRESS);
    // it('TEST CASE 01: nodeServer should have getHTTPServer method and return httpServer as object', () => {
    //     const httpServer = nodeServer.getHTTPServer();
    //     expect(typeof httpServer).toBe('object');
    // });
});
